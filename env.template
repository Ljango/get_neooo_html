# Neo4j 连接配置模板
# 复制此文件为 .env 并填入实际值
# ====================

# Neo4j 连接URI（可选，如果设置会覆盖下面的 DB_SCHEME/DB_HOST/DB_PORT 配置）
# 如果设置了 NEO4J_URI，下面的 DB_SCHEME/DB_HOST/DB_PORT 将被忽略
# NEO4J_URI=bolt://localhost:7687

# 连接协议 (bolt 或 neo4j)
DB_SCHEME=bolt

# Neo4j 服务器地址
DB_HOST=localhost

# Neo4j Bolt 端口（默认7687）
DB_PORT=7687

# 数据库用户名
DB_USER=neo4j

# 数据库密码（请修改为实际密码）
DB_PASSWORD=your_password_here

# 数据库名称（Neo4j 4.x+ 支持多数据库）
# 默认数据库名为 neo4j，也可以创建自定义数据库
DB_NAME=neo4j
