# 知识图谱数据审校系统 - 环境变量配置示例
# 复制此文件为 .env 并修改相应值
# 
# 重要提示：
# 1. 生产环境请务必修改所有默认密码
# 2. JWT密钥必须足够长且随机
# 3. 切勿将此文件提交到版本控制系统

# ========== 运行模式 ==========
# 设置为 true 启用生产模式（启用各种保护机制）
PRODUCTION=true

# ========== 数据库配置 ==========
# 数据库类型: sqlite (本地开发) 或 mysql (生产环境)
DB_TYPE=mysql

# MySQL配置
MYSQL_HOST=mysql
MYSQL_PORT=3306
MYSQL_USER=kg_user
MYSQL_DATABASE=knowledge_graph

# MySQL密码（请修改为强密码！）
MYSQL_ROOT_PASSWORD=your_strong_root_password_here
MYSQL_PASSWORD=your_strong_kg_password_here

# ========== 安全配置 ==========
# JWT密钥（生产环境请使用强密钥，至少32字符）
# 可以使用 openssl rand -hex 32 生成随机密钥
JWT_SECRET_KEY=your-very-long-secret-key-at-least-32-characters-change-this

# ========== Neo4j配置（可选）==========
# 如果使用 Neo4j 图数据库
NEO4J_PASSWORD=your_neo4j_password

# ========== 服务配置 ==========
# 应用监听端口
APP_PORT=8000

# Nginx端口（使用 Nginx 时）
NGINX_PORT=80

# ========== 日志配置 ==========
# 日志级别: DEBUG, INFO, WARNING, ERROR
LOG_LEVEL=INFO
